# iWebPlayer-S dev v1.3.6

## v1.3.6（跟进上游 v1.3.6）

- ✨ **上游 v1.3.6-A**：内联"在线资源"地球图标 → `SVG_ICONS.online`（纯重构）。
- ✨ **上游 v1.3.6-B（仅手机端）**：头部新增「在线资源 / 曲库搜索」快捷图标，点一次进入、再点一次原路返回；沉浸播放时隐藏；并从歌单下拉移除这两项。
  - ⚠️ **宽屏（分栏）刻意不显示**：图标位于 `.header-controls`（分栏下本就隐藏），宽屏由工具栏「本地/在线」开关承担（用户确认的取舍：更简约、少一步）。
  - ⚠️ 上游同批 `#device-container{min-width:75px}` **已限定为非分栏**，避免顶宽最窄分栏的设备列。
- 📌 **上游 v1.3.6-C（列表过滤 / 跳转源）暂不跟进**：依赖我们尚未移植的"本地过滤"UI，详见 `docs/IWP-1.3.5-1.3.6上游跟进评估.md`。


这是 `dev` 分支的预发布版本，用于验证尚未合入 `main` 稳定版的宽屏界面、Android WebView、播放体验改进和逐字歌词（KTV）引擎。

## 相比 main 的调整
- ✨ 优化了安卓端返回逻辑，不再弹出 "修改服务器/退出"对话框。修改服务器功能增加在首页菜单栏。
- ✨ 宽屏（分栏）模式三栏工具栏
- ✨ **新增逐字歌词（KTV）引擎 -此功能对于移动端（网页及安卓端来说，只能说聊胜于无，时间轴试了很多方案都没有原生安卓APP准确）**
- ✨ 移植上游 60FPS `requestAnimationFrame` 同步循环，支持 `[[mm:ss.xx]]` 双括号和 `[mm:ss.xxx]` 单括号两种逐字时间戳格式，支持渐变扫光（普通/平板）和逐字跳动流光（手机沉浸模式）两种渲染模式。
- 修复主题模式切换，以及 Android WebView 下深色和浅色显示不一致的问题。
- 修复 APK 中音源相关脚本的导入与加载。
- 优化静默刷新流程和专辑封面缓存，减少重复请求与封面闪烁。
- 优化 MIoT 音箱推送流程和播放状态处理。
- 调整桌面与平板横屏播放页为封面区约 32%、歌词区约 68% 的布局。
- 调整播放时间显示，改善控制栏中的进度信息可读性。
- 修复宽屏控制栏间距和分栏边界对齐。
- 🐞 修复宽屏未登录时看不到「重新登录」提示：氛围遮罩层（z-index:135）盖住了承载提示的 `#loading`，现已在弹窗状态下关闭遮罩并让提示独占整页。
- 🐞 修复宽屏右侧歌曲列表底部悬空、最后一张卡片被截断：右栏不再错误扣除只存在于左半栏的底部播放条高度。
- ✨ 跟进上游 v1.3.2：小爱音箱支持倍速调节与进度条拖拽（走 MIoT `player/speed`、`player/seek` 接口，音箱最高 2.0x）。
- 🐞 修复音箱播放进度回弹：虚拟时钟加入最高水位线防回滚、暂停恢复时重置时间锚点，并在拖拽期间锁定界面。
- 🐞 修复洛雪音质设置不生效：音质选择改为写入统一配置沙盒（此前写入的 localStorage key 无人读取），并按洛雪 3.7.8 版本校验后回写配置。
- 🐞 修复安卓 App 后台播放中断：切到后台后一首歌播完就停、重新打开才继续。渲染进程不再被系统冻结（`setRendererPriorityPolicy`），并新增媒体前台服务保活（暂停后 60 秒才退出前台）。
- ✨ 跟进上游 v1.3.3 / v1.3.5：沉浸页下拉 1:1 跟手（氛围幕布同步下移，宽屏分栏下自动禁用）、顶部把手改为下箭头、歌词拖动 1 秒强制回中、收藏改为内存更新（不再触发全库重载）、播放按钮首次点击修复、移除标题红心、修复 iOS 27 PWA 顶部虚化。
- 🐞 Token 失效自动重试：不再永久缓存 token，接口返回 401 时会重读宿主写回的最新 token 并重放一次请求（不新增免鉴权接口，避免 token 泄露）。
- 更新代码版本探针与静态脚本内容哈希，确保客户端能发现并加载新版本资源。

- ✨ 宽屏工具栏整体并入顶部那一行（原「本机 / 小爱音箱」所在行），右半栏对齐、顶部只占一行，列表多出约 59px 高度。
- ✨ 宽屏「在线资源搜索」与「本地曲库搜索」合并为一个输入框，用「本地 / 在线」开关切换，两种搜索行为与关键词记忆均保持原样。
- ✨ 新增「音源」浮层：引擎与平台（网盘服务器）直接以芯片两级选择，内含 WebDAV 检索框；在线搜索提示词随引擎变化。
- 🐞 修复宽屏下切到「在线资源」时全局 ⋯ 菜单按钮被藏进隐藏容器而消失。
- ✨ 宽屏歌单栏改为可收缩，列窄时优先完整显示歌单名（`(数量)` 先让位），不再把名字截成「所有歌…」。
- ✨ 平板分屏（约 2/3 宽）下点搜索框会自动让出歌单栏；未点开时按钮标签精简为「歌 / 单 / 源」，点开后恢复完整文字，提示文字缩小一档。
- ✨ MIoT 推送歌单更名为 `iWP-S推送`（更短）：升级后会自动把旧的 `iWebPlayer-S推送` **直接改名**（走宿主重命名接口，不删歌单、不打断播放），并同步界面；推送时也会清理历史遗留的重复歌单。

## 本次构建

下方提交列表由发布工作流根据当前 `dev` 分支自动追加。此文件是预发布正文的固定来源，后续构建必须保留以上说明。

## 使用说明

1. 下载本预发布版本中的 APK 或 SongLoft 插件包。
2. 安装前请备份当前配置，并确认可以接受 dev 版本可能存在的行为变化。
3. 发现问题时，请附上设备、浏览器或 Android 版本、复现步骤和相关截图。
