# iWebPlayer-S dev v1.1.6

这是 `dev` 分支的预发布版本，用于验证尚未合入 `main` 稳定版的宽屏界面、Android WebView、播放体验改进和逐字歌词（KTV）引擎。

## 近期优化（追加）

### ✨ 宽屏（分栏）模式三栏工具栏

- **左栏**：在线资源搜索（搜歌/搜单 + 音源/平台切换），默认 32%，聚焦展开到 48%。
- **中栏**：本地曲库搜索，36%，聚焦自动进入「曲库搜索」模式；提示文字改为「本地曲库搜索」并增加放大镜图标。
- **右栏**：歌单选择下拉（在线资源/曲库搜索/我的歌单）+ 列表操作菜单，32%。
- **手机端工具栏完全保持不变**，分栏与手机共用同一批真实 DOM 控件（搬移而非代理），原有交互与函数全部复用。
- 「所有歌曲」按添加时间倒序排列（最新在前）。

### 🛠 分栏工具栏修复

- 修复搜歌/搜单/音源/平台按钮点击后左栏收起、点击无效的问题（保持输入框焦点 + relatedTarget 判断）。
- 修复歌单下拉、音源/平台下拉被裁剪、定位错乱的问题（`overflow: visible` + 定位祖先）。
- 修复中栏曲库搜索在任意歌单下输入/回车无效的问题（聚焦/回车自动切「曲库搜索」模式）。
- 修复底部横向滚动条（`overflow-x: hidden` + `min-width: 0`）。
- 暗色模式工具栏卡片与浅色观感一致（去掉半透明方框/黑边投影）。

### 📱 Android 壳层

- **开屏 Logo 透明化**：系统开屏单独使用透明四角图标（`ic_splash`）+ 深色背景，去掉四角黑边；桌面图标保持「原图满幅版」不变（启动器遮罩下观感正常）。
- **边缘滑动返回**：应用内从屏幕左边缘向右滑、右边缘向左滑，触发「返回」（等同系统返回键：网页后退 / 播放器页弹修改服务器/退出对话框 / 退出）。

## 相比 main 的调整

- ✨ **新增逐字歌词（KTV）引擎**：移植上游 60FPS `requestAnimationFrame` 同步循环，支持 `[[mm:ss.xx]]` 双括号和 `[mm:ss.xxx]` 单括号两种逐字时间戳格式，支持渐变扫光（普通/平板）和逐字跳动流光（手机沉浸模式）两种渲染模式。
- 修复主题模式切换，以及 Android WebView 下深色和浅色显示不一致的问题。
- 修复 APK 中音源相关脚本的导入与加载。
- 优化静默刷新流程和专辑封面缓存，减少重复请求与封面闪烁。
- 优化 MIoT 音箱推送流程和播放状态处理。
- 调整桌面与平板横屏播放页为封面区约 32%、歌词区约 68% 的布局。
- 调整播放时间显示，改善控制栏中的进度信息可读性。
- 修复宽屏控制栏间距和分栏边界对齐。
- 更新代码版本探针与静态脚本内容哈希，确保客户端能发现并加载新版本资源。

## 本次构建

下方提交列表由发布工作流根据当前 `dev` 分支自动追加。此文件是预发布正文的固定来源，后续构建必须保留以上说明。

## 使用说明

1. 下载本预发布版本中的 APK 或 SongLoft 插件包。
2. 安装前请备份当前配置，并确认可以接受 dev 版本可能存在的行为变化。
3. 发现问题时，请附上设备、浏览器或 Android 版本、复现步骤和相关截图。
