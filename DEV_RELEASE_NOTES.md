# iWebPlayer-S dev 预发布说明

这是 `dev` 分支的持续预发布版本。该版本用于验证宽屏设备、Android WebView 壳和播放器交互优化，可能包含尚未进入稳定版的改动。

## 当前功能范围

- 宽屏设备适配：平板、横屏设备和桌面浏览器使用左右双栏首页。
- 宽屏播放页：支持专辑封面、歌曲信息、动态歌词和底部播放控制。
- 播放体验：支持播放、歌单、歌词、封面刮削、倍速、续播和收藏。
- 在线资源：支持 LXMusic 与 WebDAV 搜索及播放。
- 歌词获取：按 SongLoft 主程序、LXMusic、刮削服务的顺序获取歌词。
- Android APK：使用 WebView 壳加载服务器端插件页面，支持局域网 HTTP、锁屏媒体控制和播放进度。
- 缓存更新：构建时为静态脚本注入内容哈希，避免客户端长期使用旧资源。

## 本次构建

下方提交列表由发布工作流根据当前 `dev` 分支自动追加。此文件是固定说明的唯一来源，后续构建不得覆盖或删除以上内容。

## 使用说明

1. 下载本预发布版本中的 APK 或 SongLoft 插件包。
2. 安装前请备份当前配置，并确认可以接受 dev 版本可能存在的行为变化。
3. 发现问题时，请附上设备、浏览器或 Android 版本、复现步骤和相关截图。
